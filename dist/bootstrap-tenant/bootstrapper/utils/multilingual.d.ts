export declare const translationFieldIdentifier = "this is a safe string used for identifying translation fields in the spec";
export declare function trFactory(language: string): (value: any, id: string) => {
    [x: string]: any;
    "this is a safe string used for identifying translation fields in the spec": string;
};
export declare function mergeInTranslations(targetThing: any, sourceThing: any): any;
